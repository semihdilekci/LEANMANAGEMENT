name: Terraform Apply (staging)

on:
  push:
    tags:
      - 'tf-staging-*'

permissions:
  contents: read
  id-token: write

concurrency:
  group: terraform-apply-staging
  cancel-in-progress: false

jobs:
  apply:
    runs-on: ubuntu-latest
    if: ${{ secrets.AWS_TERRAFORM_ROLE_ARN_STAGING != '' && secrets.TF_BACKEND_BUCKET_STAGING != '' }}
    environment: staging
    defaults:
      run:
        working-directory: infrastructure/terraform/environments/staging
    env:
      AWS_REGION: eu-central-1
    steps:
      - uses: actions/checkout@v4

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: '1.8.5'

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_TERRAFORM_ROLE_ARN_STAGING }}
          aws-region: eu-central-1

      - name: Terraform Init
        run: |
          terraform init -input=false \
            -backend-config="bucket=${{ secrets.TF_BACKEND_BUCKET_STAGING }}" \
            -backend-config="key=staging/terraform.tfstate" \
            -backend-config="region=eu-central-1" \
            -backend-config="encrypt=true" \
            -backend-config="dynamodb_table=${{ secrets.TF_BACKEND_LOCK_TABLE_STAGING }}"

      - name: Terraform Apply
        run: terraform apply -auto-approve -input=false
        env:
          TF_VAR_acm_certificate_arn: ${{ secrets.TF_VAR_ACM_CERTIFICATE_ARN_STAGING }}
          TF_VAR_github_repository: ${{ secrets.TF_VAR_GITHUB_REPOSITORY }}
          TF_VAR_terraform_remote_state_bucket_arn: ${{ secrets.TF_VAR_TERRAFORM_REMOTE_STATE_BUCKET_ARN_STAGING }}
          TF_VAR_terraform_remote_state_lock_table_arn: ${{ secrets.TF_VAR_TERRAFORM_REMOTE_STATE_LOCK_TABLE_ARN_STAGING }}
          TF_VAR_terraform_state_kms_key_arn: ${{ secrets.TF_VAR_TERRAFORM_STATE_KMS_KEY_ARN_STAGING }}
